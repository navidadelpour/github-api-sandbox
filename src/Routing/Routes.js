export const index =        "/"
export const admin =        `${index}admin/`

export const repositories = `${admin}repositories/`

export const followers = `${admin}followers/`
export const followings = `${admin}followings/`
